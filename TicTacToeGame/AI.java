public class AI extends Player{
    public boolean intelligence;

    public AI(int symbol, String name) {
        super(symbol, name);
        
    }

    //if name = weak, use random moves else if name = smart use minimax
}
